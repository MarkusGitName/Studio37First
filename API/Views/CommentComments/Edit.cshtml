@model API.Model.CommentComments

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>CommentComments</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="OldComment" class="control-label"></label>
                <select asp-for="OldComment" class="form-control" asp-items="ViewBag.OldComment"></select>
                <span asp-validation-for="OldComment" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NewComment" class="control-label"></label>
                <select asp-for="NewComment" class="form-control" asp-items="ViewBag.NewComment"></select>
                <span asp-validation-for="NewComment" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
