@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.WebAssembly.Authentication
@using Studio37Media.Shared.ViewModels


@using System.IO
@using BlazorInputFile
@using System.IO.MemoryMappedFiles


@attribute [Authorize]
@inject HttpClient Http

@code {

    PromoPhoto newPromoPhoto = new PromoPhoto();

    async void HandleFormSubmit()
    {

        // upload photos
        //await Http.PostAsync("FileUpload", ThumbnailContent);

        newPromoPhoto.id = Guid.NewGuid();
        await Http.PostAsJsonAsync<PromoPhoto>("PromoPhoto/NewPromoPhoto", newPromoPhoto);
    }

}
